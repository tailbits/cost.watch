---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<div class={`${className} flex items-center gap-2 font-semibold`}>
  <span class="text-green-600 text-xl font-bold">$</span>
  <span class="text-gray-900">cost.watch</span>
</div>
